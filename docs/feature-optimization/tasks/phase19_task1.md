# Phase 19 Task 1: 重组项目树扩展配置层级

## 目标

将项目树中的扩展配置从“类别节点下直接列出配置”调整为“类别节点 -> 具体扩展节点 -> 配置项”，让配置所有权在树上可见。

## 实施

1. 调整项目树全局资源构建
   - 扩展配置不再直接挂在类别节点下
   - 按扩展类型建立中间节点
2. 调整扩展配置上下文菜单入口
   - `新建配置` 迁移到具体扩展节点
   - 配置项继续保留复制、导出、设默认等动作
3. 保持数据模型不变
   - 不修改 `global_assets` 的持久化字段
   - 只重组树展示和命令上下文

