# Phase 33 Task 1: 曲线样式默认标签替换

## 目标

将曲线样式配置中的“无”统一替换为“默认样式”。

## 涉及文件

| 文件 | 操作 |
|---|---|
| `ui/pages/chart_page.py` | 下拉框/标签文案调整 |
| `ui/widgets/*` | 如有共享样式选择组件，同步调整 |

## 验收清单

- [ ] 曲线样式 UI 不再显示“无”
- [ ] 行为与原空值语义一致
