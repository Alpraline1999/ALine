# Phase 18 Task 3: 收口未保存关闭弹窗底部布局白边

## 目标

修复 phase18 关闭确认弹窗下半部分的白边问题，确保弹窗仍使用统一的消息框底部按钮区样式，不出现额外内容区留白。

## 实施

1. 调整弹窗布局
   - 将“保存 / 不保存 / 取消”迁移到 `MessageBoxBase` 的底部按钮区
   - 保留标题和说明文本在内容区
2. 保持三态返回不变
   - 不修改 `ProjectCloseDecision` 语义
   - 不改动主窗口、项目树和数字化页的调用方式
3. 小范围验证
   - 使用项目自带 `.venv` 跑相关窄测
   - 优先验证弹窗语法和 close-path 行为

