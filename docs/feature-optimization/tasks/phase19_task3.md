# Phase 19 Task 3: 扩展配置树修复收口与窄测确认

## 目标

完成 phase19 的最后收口，确认扩展配置树已经按“类别 -> 扩展 -> 配置项”分层展示，并且新建、复制、隐藏项过滤等关键路径在窄范围测试中通过。

## 实施

1. 修复扩展配置对象查找
   - `build_extension_entry()` 只接收真实扩展对象
   - 新建/默认名称逻辑从注册表按类别与类型取回扩展实体
2. 对齐扩展配置管理动作
   - 新建配置恢复为输入命名
   - 复制配置使用当前 `global_assets` 的扩展配置写入接口
3. 做窄范围验证
   - 仅运行 `ProjectTreeWidget` 扩展配置相关测试
   - 保持不做全量回归

## 结果

- 已完成。
- 窄范围验证通过。
