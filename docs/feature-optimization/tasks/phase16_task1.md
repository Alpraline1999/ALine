# Phase 16 Task 1: 修复可视化页抽样接口回归

## 目标

修复 `chart_page.py` 大点数预览路径中对 `decimate_xy_for_rendering()` 的旧签名调用，恢复树节点激活后的可视化绘图流程。

## 实施

1. 统一抽样调用方式
   - 将 `_decimated_redraw()` 中的旧关键字参数调用改为 policy 风格
   - 确保返回值数量与消费方式一致
2. 保持重绘行为不变
   - 仅修复接口漂移，不调整大点数预览触发阈值和重绘节奏
3. 补充窄测试
   - 覆盖大点数预览路径的抽样调用
   - 覆盖树节点激活后可视化页不再抛出 `TypeError`

