# Phase 33 Task 2: 绘图样式默认标签替换与兼容验证

## 目标

将绘图样式配置中的“无”统一替换为“默认样式”，并验证旧项目/旧模板兼容。

## 涉及文件

| 文件 | 操作 |
|---|---|
| `ui/pages/chart_page.py` | 绘图样式选择文案调整 |
| `core/global_assets.py` | 如有展示层标签映射，复核一致性 |
| `tests/` | 补旧模板和旧项目兼容回归 |

## 验收清单

- [ ] 绘图样式 UI 不再显示“无”
- [ ] 旧项目加载后无行为差异
- [ ] 模板预览和应用结果一致
